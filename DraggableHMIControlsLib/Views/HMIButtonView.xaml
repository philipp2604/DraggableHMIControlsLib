<local:HMIControlView x:Class="DraggableHMIControlsLib.Views.HMIButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DraggableHMIControlsLib.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="250">

    <Button x:Name="button" Width="{Binding Width}" Height="{Binding Height}" Foreground="{Binding ForegroundBrush}" Background="{Binding BackgroundBrush}" BorderBrush="{Binding BorderBrush}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
        <TextBlock TextWrapping="Wrap" Text="{Binding Text}" FontSize="{Binding FontSize}" FontFamily="{Binding FontFamily}" />
        <Button.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="{Binding CornerRadius}"/>
            </Style>
        </Button.Resources>
    </Button>

</local:HMIControlView>
